import React from 'react';
import './styles.scss';

interface Props {
};

export const Contact = ({  }: Props) => {
    return(
        <div className="contact-container">
            contact
        </div>
    );
};
