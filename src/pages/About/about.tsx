import React from 'react';
import './styles.scss';

interface Props {
};

export const About = ({  }: Props) => {
    return(
        <div className="about-container">
            about
        </div>
    );};
