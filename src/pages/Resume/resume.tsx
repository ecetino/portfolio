import React from 'react';
import './styles.scss';

interface Props {
};

export const Resume = ({  }: Props) => {
    return(
        <div className="resume-container">
            Resume
        </div>
    );
};
