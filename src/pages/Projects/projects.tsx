import React from 'react';
import './styles.scss';

interface Props {
};

export const Projects = ({  }: Props) => {
    return(
        <div className="projects-container">
            projects
        </div>
    );
};
